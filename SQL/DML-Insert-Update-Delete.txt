-- 🔹 INSERT Examples
--  1. Insert a new job
INSERT INTO jobs (job_id, job_desc, min_lvl, max_lvl)
VALUES (1, 'Junior Developer', 10, 100);
--  2. Insert another job
INSERT INTO jobs (job_id, job_desc, min_lvl, max_lvl)
VALUES (2, 'Senior Developer', 100, 200);
-- 🔹 UPDATE Examples
--  3. Update job description
UPDATE jobs
SET job_desc = 'Software Developer'
WHERE job_id = 1;
--  4. Update max level
UPDATE jobs
SET max_lvl = 250
WHERE job_id = 2;
-- 🔹 DELETE Examples
--  5. Delete a specific job
DELETE FROM jobs
WHERE job_id = 1;
--  6. Delete jobs with low max level
DELETE FROM jobs
WHERE max_lvl < 150;